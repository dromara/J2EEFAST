<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fast.framework.sys.dao.SysDictTypeDao">

	<sql id="selectDictTypeVo">
        select dict_id, dict_name, dict_type, status, create_by, create_time, remark
		from sys_dict_type
    </sql>

	<select id="checkDictTypeUnique" parameterType="String" resultType="com.fast.framework.sys.entity.SysDictTypeEntity">
		<include refid="selectDictTypeVo"/>
		where dict_type = #{dictType}
	</select>

	<update id="updateDictType" parameterType="com.fast.framework.sys.entity.SysDictTypeEntity">
		update sys_dict_type
		<set>
			<if test="dictName != null and dictName != ''">dict_name = #{dictName},</if>
			<if test="dictType != null and dictType != ''">dict_type = #{dictType},</if>
			<if test="isSys != null and isSys !=''">is_sys = #{isSys},</if>
			<if test="status != null">status = #{status},</if>
			<if test="remark != null">remark = #{remark},</if>
			<if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
			update_time = sysdate()
		</set>
		where dict_id = #{dictId}
	</update>

	<delete id="deleteDictTypeByIds" parameterType="Long">
		delete from sys_dict_type where dict_id in
		<foreach collection="array" item="dictId" open="(" separator="," close=")">
			#{dictId}
		</foreach>
	</delete>
</mapper>